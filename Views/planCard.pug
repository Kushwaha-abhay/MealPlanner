.signup-child
  .signup-header
    h3 #{plan.name}
    .signup-pricing
      span.pricing Rs.#{plan.price}
      | /year
      p That’s only Rs.#{Math.ceil(plan.price/plan.duration)} per meal
  .signup-features
    ul
      li
        i.fas.fa-check.small-icon  
        | 1 meal every day
      li
        i.fas.fa-check.small-icon  
        | Order 24/7
      li
        i.fas.fa-check.small-icon  
        | Access to newest creations
      li
        i.fas.fa-check.small-icon  
        | Free delivery
      li
        i.fas.fa-check.small-icon  
        | Rated #{plan.rating} out of 5
    .signup-button
      a.btn.btn-full(href='#', planId=`${plan["_id"]}`) Subscribe
